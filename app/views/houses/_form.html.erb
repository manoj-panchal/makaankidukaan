<%= form_with(model: house, local: true) do |form| %>
  <% if house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(house.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
      <% house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lat %>
    <%= form.text_field :latitude, id: :house_lat %>
  </div>

  <div class="field">
    <%= form.label :long %>
    <%= form.text_field :longitude, id: :house_long %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :house_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
